# Night Shift Quality Scorecard

This scorecard keeps the rebuild honest. A number only moves when there is
repeatable code, test evidence, or an end-to-end run artifact behind it.

## Current Regrade: 2026-07-12

The target is **95/100 in every dimension at the same time**. Scores describe
proven user outcomes, not the amount of code present. The July 11 overnight
run remains the broad baseline: 4 repos, 69 batches, 40 attempted findings,
0 accepted findings, and about 181,159 estimated tokens. A July 12 bounded
rehearsal produced 1 evidence-backed candidate in 1 local call and 2,887
estimated tokens; independent review confirmed the missing direct contract
test and added it to the suite.

| Dimension | Score | Evidence | What reaches 95 |
| --- | ---: | --- | --- |
| Product idea | 95 | Idle local compute becomes a morning decision surface. | Preserve the promise while proving repeated value. |
| One-command repeat use | 92 | A blank-home saved setup was followed by a real zero-question `start --yes --once` cycle in about seven seconds: exit 0, byte-identical config, no second setup ledger, truthful routing, and a morning brief. This was one cycle, not an eight-hour soak or native slash invocation. | Prove the native skill invocation starts and completes a full healthy eight-hour shift with zero repeat questions. |
| Installation | 76 | Linked and copied installs work on this Mac; copied installs now include the pinned runner context and package checks verify it. | Fresh-machine macOS and Linux install proof with automatic PATH handling. |
| First-run UX | 80 | Blank-home runs automatically detect repo/compute, show the safe plan, and support one-consent setup; the repeat proof now removes misleading LAN language under `mac-only`. A real new-user comprehension study is still missing. | Observe brand-new users understanding and completing the one-consent flow without help. |
| Hardware detection | 74 | Ollama and configured Windows endpoints are detected and chat-probed. Real trust preflights now distinguish a compatible Python runner from Draft's unavailable `swiftc` tooling before autonomy is saved. | Prove LM Studio, Ollama, offline fallback, LAN discovery, reconnect behavior, and a Swift-capable runner path. |
| Minimal setup | 84 | Setup requires no model URL/name or GitHub selection. A real blank-home repeat reused saved settings without rewriting config or adding setup artifacts; trust-repo separately proves one-consent compatible autonomy and clear incompatible-runner recovery. | Repeat on fresh macOS/Linux users and prove clear recovery when no eligible repo or AI is available. |
| Useful output | 52 | Three strict fresh Draft trials all produced a validator-clean MAYBE for open issue #38 and the exact disabled flag at `ParakeetEngine.swift:51`; two passed on the first call. No approved repo profile existed, so no repair or runtime proof was attempted. | Varied healthy overnight runs produce independently verified useful outcomes without duplicate churn. |
| Task selection | 85 | Deterministic ranking now skips trackers, uses explicit backticked symbols and exact paths, ranks files by matched-symbol count, and preserves signal strength through feedback ordering. A fresh Draft run selected issue #38 and its correct speech file ahead of broad release and README issues. | Demonstrate high accepted-outcome rates across varied live GitHub signals and repos. |
| Repository prioritization | 78 | A live 30-day regrade reduced BetterFeedback from 1,355 to 555 by filtering 14 branch failures to three default-branch failures and capping 27 green drafts. Transcripted moved above Draft's backlog, and the explicit Night Shift repo is guaranteed a selected slot. | Measure that selected repos consistently match accepted user value across overnight runs. |
| GitHub usefulness | 82 | PR, issue, failed-workflow, and recent-repo signals feed queues. Live ranking now distinguishes actionable failed/review work from stale branch and green-draft volume; live Draft issue #38 also became an exact one-file candidate. | Correlate selected work to accepted PR/issue outcomes across owned repos. |
| Portfolio discovery | 82 | Live authenticated discovery selected `r3dbars/BetterFeedback`, cloned an owner-validated private cache, and rejected symlink, path-escape, foreign-owner, and mismatched-origin cases in tests. | Add user-controlled owner/repo priorities and quiet hours, then prove varied-account behavior. |
| Task specificity | 84 | A live Windows repair was constrained to one exact source file, one reproduced assertion, one approved argv command, and one expected result. | Exercise it on varied real repositories. |
| Multi-repo operation | 76 | Portfolio cycles, stop limits, and a controller lock exist. Live ten-repo discovery now ranks actionable signals with capped backlog effects and guarantees the user's explicit primary repo remains in the selected set. | Test useful verified outcomes across real mixed-language portfolio runs. |
| Mac compute | 92 | Mac-local `qwen/qwen3-coder-next` generated a real one-file repair through the local lane; rootless Podman promoted it to `PROVEN_REPAIR` with the source untouched. | Dynamic queueing, thermal/resource feedback, and varied real-repo repairs. |
| Windows compute | 90 | The live LAN `qwen3-coder:30b` worker produced a one-file repair through two bounded calls; real Podman verification promoted it to `PROVEN_REPAIR`. | Authenticated endpoint validation, reconnect tests, and varied real-repo repairs. |
| Efficiency | 80 | Weak portfolio work can be filtered to zero tokens. Three strict Draft trials accepted 3/3 candidates, two on the first call, averaging about 5,184 tokens per accepted candidate versus about 7,700 before the issue-specific contract. | Prove strong tokens-per-accepted-outcome across full overnight runs with revision caching. |
| Evidence quality | 94 | Three strict Draft trials each produced one exact line-51 citation and a validator-clean candidate. Issue prompts require one literal claim/citation, and the validator now rejects unsupported claims of intent. Results remain MAYBE because source evidence is not execution proof. | Exercise reproduction evidence across varied real repositories. |
| Deterministic proof | 94 | A real Windows-model plus rootless-Podman rehearsal reproduced a failure, applied one approved patch in tmpfs, passed afterward, removed the worktree, and left the source repo clean. | Repeat across diverse repositories and prove provider restart recovery. |
| Patch autonomy | 93 | Live Mac and Windows models each generated a one-file repair with deterministic sandbox proof. A real external-approval rehearsal now removes manual repo-file editing while preserving one explicit owner consent, immutable runner binding, and disabled GitHub writes. | Prove model-generated isolated patches across diverse real repositories. |
| Draft PR creation | 78 | Real Docker-backed publication opened draft PR #24 from an exact-main SHA after 9 focused safety tests; GitHub confirmed draft status and one approved file, the worktree was removed, replay was rejected, and nothing merged or deployed. This was a harmless rehearsal, not useful project work. | Produce independently useful tested draft PRs across varied repos during unattended runs without duplicate churn. |
| Brief safety | 92 | No host repo execution; restricted actions and redaction. | Continue adversarial prompt-injection testing. |
| Execute-draft safety | 95 | Real runner proofs cover read-only source mounts, no-network tmpfs patching, exact changed-path validation, and cleanup. External approval now rejects an unadvertised candidate before execution and admits an advertised candidate to a passing 174-test baseline sandbox while leaving the source clean. | Preserve adversarial Docker/Podman proof before widening access. |
| Prompt and secret security | 88 | Redaction, protected paths, code-first context selection, and explicit untrusted-data boundaries. | Add a prompt-injection corpus and real-run measurements. |
| Morning UX | 72 | Empty grounded runs now say that nothing had enough evidence and will be retried after new activity; they no longer blame healthy compute setup. Direct replay showed matching YELLOW terminal/report status, startup GREEN, zero calls, and a factual repo surface. | Validate morning comprehension and review effort across varied real overnight outcomes. |
| Cloud-agent handoff | 35 | A real morning item produced a redacted local pack; the real no-consent run was denied, while 70-test coverage proves read-only invocation and response validation. | Obtain explicit consent and prove a live Codex review, then repeat across varied items without privacy leaks. |
| Reliability | 89 | Stop deadlines, locks, migrations, and task transitions are tested. A real isolated process group plus queued future was cancelled and cleaned up in five consecutive integration runs; approval proofs also cover revision rejection and runner failure. Controller crash restart and concurrent scheduler recovery remain unproven. | Prove controller crash recovery and concurrent scheduler integration, then repeat under a long soak. |
| Observability | 94 | Ledgers, attempts, skips, metrics, health, and sandbox capability are explicit. The real repeat proof caught and fixed child routing drift: privacy, wake goal, mode, guidance, permission, and stop now match saved parent settings. | Confirm retention behavior and routing truth in a real 10-hour soak. |
| Learning loop | 55 | Feedback now changes pre-model selection by repo and task family: positive votes prioritize, one negative downranks, and two negatives suppress Normal-mode work without bypassing grounding. No real user-feedback outcome lift is proven yet. | Demonstrate better accepted outcomes and lower wasted tokens across later real user-rated runs. |
| Test coverage | 95 | All 234 tests and package/install checks pass on the host; the complete suite also passes in the real no-network Docker VM. Coverage directly exercises queue, dispatch, lifecycle, real process cancellation, protected process-group IDs, setup, admission, evidence bounds, Git revisions, preview, portfolio scoring, publication, and cleanup. | Preserve this gate while adding controller-restart and concurrent-scheduler integration cases. |
| Maintainability | 91 | Queue construction, model dispatch, deadlines, cancellation, cleanup eligibility, and active-controller policy now have cohesive tested module owners. Real process cancellation passed five consecutive runs. The controller fell from 4,785 to 3,923 lines but still owns run/autopilot orchestration. | Split run/autopilot orchestration and controller-state transitions into tested modules. |
| Portability | 92 | The immutable runner now passes the complete package/install gate and verifies a real draft through Docker in a Colima macOS VM; prior ARM64 Podman proof remains valid, while current Podman restart behavior is unstable. | Prove Linux installation and provider restart/recovery across fresh machines. |
| Ten-hour readiness | 88 | Fixed stop limits, lock, cooldown, unattended pause, retention controls, and a per-revision rejection circuit breaker exist. Real process-group cancellation and reviewed-ledger cleanup eligibility now pass repeatable integration tests. | A clean 10-hour soak with restart, resource, and disk evidence. |
| Honesty about proof | 95 | Worker claims stay separate from deterministic and manual proof. Two live terminal/report mismatches are fixed and replayed: portfolio and child runs now agree on YELLOW, unproven claims are not repeated as findings, and a no-work result no longer implies healthy compute is broken. | Preserve this distinction through real draft-PR and cloud-handoff flows. |

## Non-Negotiable Promotion Rules

1. A score never increases from a model claim alone.
2. A repo is analysis-only until its owner supplies a reviewed local or remote-bound external profile.
3. A task becomes patchable only after a sandboxed reproduction and immutable verification plan.
4. An overnight run opens only tested draft PRs after explicit saved consent;
   it never merges, deploys, or releases.
5. Every score must reach 95 simultaneously; code paths, mocks, and model claims alone do not qualify.
6. Safety and privacy may not be weakened to improve usefulness or autonomy.
7. Hardware, LAN, GitHub writes, scheduling, recovery, and overnight usefulness require real integration proof.
